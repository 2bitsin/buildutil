// Package smoke for @PKG_NAME@: compiling against the shipped headers
// and linking the shipped libraries IS the test the scaffold provides.
// Extend it with real API calls from the library.
int main() { return 0; }
